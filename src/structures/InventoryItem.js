class InventoryItem {
    /**
     *
     * @param {any} item
     * @param {number} count
     */
    constructor(item, count) {
        this.item = item;
        this.count = count;
    }
}

module.exports = InventoryItem;