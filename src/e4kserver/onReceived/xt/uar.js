module.exports = {
    name: "uar",
    /**
     * @param {Socket} socket
     * @param {number} _
     * @param {object} __
     */
    execute(socket, _, __) {
        if(socket.debug) console.log("uar, uap, ufa, ufp, gms, gcl, kgv, gkl, opt, vip and gcu not fully correctly working");
    }
}